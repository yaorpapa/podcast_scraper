name: Daily Web Crawler

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  run-crawler:
    runs-on: windows-latest
    env:
      PYTHONIOENCODING: utf-8  # 強制 Python 使用 UTF-8 編碼
      
    steps:
      # 1. 下載 Repository 內的程式碼
      - name: Checkout Repository
        uses: actions/checkout@v3

      # 2. 設定 Python 環境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # 3. 安裝相依套件（請確認 requirements.txt 存在且內容正確）
      - name: Install dependencies
        run: pip install -r requirements.txt

      # 5. 執行你的爬蟲程式
      - name: Run Crawler
        run: python podcast_scraper.py
# 6. 保留更新後的 podcasts.db 檔案
       # - name: Upload podcasts.db artifact
 # uses: actions/upload-artifact@v4
 # with:
 #  name: podcasts-db
  #  path: podcasts.db

